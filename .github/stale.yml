# Source: https://github.com/probot/stale#usage
# Disabled for now, per https://github.com/ScoopInstaller/Extras/discussions/7094#discussioncomment-1556836

# Configuration for probot-stale - https://github.com/probot/stale

# Number of days of inactivity before an Issue or Pull Request becomes stale
# Default is 60
daysUntilStale: 99999

# Number of days of inactivity before an Issue or Pull Request with the stale label is closed.
# Set to false to disable. If disabled, issues still need to be closed manually, but will remain marked as stale.
# Default is 7
daysUntilClose: 99999

# Only issues or pull requests with all of these labels are check if stale. Defaults to `[]` (disabled)
# onlyLabels: []

# Issues or Pull Requests with these labels will never be considered stale. Set to `[]` to disable
exemptLabels:
    - blocked
    - bug
    - discussion
    - enhancement
    - help-wanted
    - package-request
    - pinned
    - security
    - upstream

# Set to true to ignore issues in a project (defaults to false)
# exemptProjects: false

# Set to true to ignore issues in a milestone (defaults to false)
# exemptMilestones: false

# Set to true to ignore issues with an assignee (defaults to false)
# exemptAssignees: false

# Label to use when marking as stale
# Default is wontfix
staleLabel: stale

# Comment to post when marking as stale. Set to `false` to disable
# Default defined at https://github.com/probot/stale/blob/7510fac863900a98057436d308e9fe21bbe314ec/lib/schema.js#L55
markComment: >
  This issue has been automatically marked as stale because it has not had
  recent activity. It will be closed if no further activity occurs. Thank you
  for your contributions.
# Comment to post when removing the stale label.
# unmarkComment: >
#   Your comment here.

# Comment to post when closing a stale Issue or Pull Request. Set to `false` to disable
# Default is `false`
# closeComment: >
#   Your comment here.

# Limit the number of actions per hour, from 1-30. Default is 30
# limitPerRun: 30

# Limit to only `issues` or `pulls`
# Default is ``
# only: issues

# Optionally, specify configuration settings that are specific to just 'issues' or 'pulls':
# pulls:
#   daysUntilStale: 30
#   markComment: >
#     This pull request has been automatically marked as stale because it has not had
#     recent activity. It will be closed if no further activity occurs. Thank you
#     for your contributions.

# issues:
#   exemptLabels:
#     - confirmed
